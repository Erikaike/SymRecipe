{% extends 'base.html.twig' %}

{% block title %}Eri-cipe - Edit user
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-4">Update your info</h1>
		{% for message in app.flashes('warning') %}
			<div class="alert alet-warning mt-4">
				{{ message }}
			</div>
		{% endfor %}
		{{form(form)}}
		<button type="submit" class="btn btn-primary mt-4 mb-4">Submit</button>
	</div>
{% endblock %}
