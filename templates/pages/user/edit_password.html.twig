{% extends 'base.html.twig' %}

{% block title %}SymRecipe - Update password
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-4">Update password</h1>
		{% for message in app.flashes('warning') %}
			<div class="alert alet-warning mt-4">
				{{ message }}
			</div>
		{% endfor %}
		{{form(form)}}
		</div>
	{% endblock %}
