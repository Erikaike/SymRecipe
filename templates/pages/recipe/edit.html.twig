{% extends 'base.html.twig' %}

{% block title %}New Eri-cipe {% endblock %}

{% block body %}
    
    <div class="text-center mt-2 mb-2">
        <h1>Update your Eri-cipe : {{recipe.name}} 😋</h1>
    </div>
    {% include "pages/recipe/_form.html.twig" %}
    <div class="text-center">
        <button type="submit" class="btn btn-secondary mt-4 bt-4">{{ button_label|default('Done 🥗') }}</button>
    </div>
    {{ form_end(form) }}
</section>
{% endblock %}
